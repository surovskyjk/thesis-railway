# Python 3.14 was used during development.
# Versions listed below are the ones the project was tested against.
# Install all dependencies with: pip install -r requirements.txt

# GUI framework
PySide6==6.10.2

# Fast interactive plots, the only plotting backend used by the GUI
pyqtgraph==0.14.0

# Numerical arrays and linear algebra
numpy==2.4.2

# Coordinate reference system transformations (LandXML reprojection)
pyproj==3.7.2

# Clothoid / Euler spiral geometry (spiral discretisation for map view)
pyclothoids==0.2.0

# Interactive map rendering
folium==0.20.0

# Colour-scale colormaps for the speed-coloured map view (folium dependency, used directly)
branca==0.8.2
